/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/program-client-core";
import { findOvenPda } from "../pdas";
import { LAST_BATCH_PROGRAM_ADDRESS } from "../programs";

export const SWEETEN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  74, 113, 13, 217, 96, 253, 141, 2,
]);

export function getSweetenDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SWEETEN_DISCRIMINATOR);
}

export type SweetenInstruction<
  TProgram extends string = typeof LAST_BATCH_PROGRAM_ADDRESS,
  TAccountDonor extends string | AccountMeta<string> = string,
  TAccountOven extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDonor extends string
        ? WritableSignerAccount<TAccountDonor> &
            AccountSignerMeta<TAccountDonor>
        : TAccountDonor,
      TAccountOven extends string
        ? WritableAccount<TAccountOven>
        : TAccountOven,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SweetenInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type SweetenInstructionDataArgs = { amount: number | bigint };

export function getSweetenInstructionDataEncoder(): FixedSizeEncoder<SweetenInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SWEETEN_DISCRIMINATOR }),
  );
}

export function getSweetenInstructionDataDecoder(): FixedSizeDecoder<SweetenInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getSweetenInstructionDataCodec(): FixedSizeCodec<
  SweetenInstructionDataArgs,
  SweetenInstructionData
> {
  return combineCodec(
    getSweetenInstructionDataEncoder(),
    getSweetenInstructionDataDecoder(),
  );
}

export type SweetenAsyncInput<
  TAccountDonor extends InstructionSignerInput = InstructionSignerInput,
  TAccountOven extends InstructionAccountInput = InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput =
    InstructionAccountInput,
> = {
  donor: TAccountDonor;
  oven?: TAccountOven;
  systemProgram?: TAccountSystemProgram;
  amount: SweetenInstructionDataArgs["amount"];
};

export async function getSweetenInstructionAsync<
  TAccountDonor extends InstructionSignerInput,
  TAccountOven extends InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput,
  TProgramAddress extends Address = typeof LAST_BATCH_PROGRAM_ADDRESS,
>(
  input: SweetenAsyncInput<TAccountDonor, TAccountOven, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SweetenInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountDonor,
      InstructionAccountInputAddress<TAccountDonor>
    >,
    ResolvedInstructionAccountMeta<
      TAccountOven,
      InstructionAccountInputAddress<TAccountOven>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LAST_BATCH_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    donor: { value: input.donor ?? null, isSigner: true, isWritable: true },
    oven: { value: input.oven ?? null, isSigner: false, isWritable: true },
    systemProgram: {
      value: input.systemProgram ?? null,
      isSigner: false,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.oven.value) {
    accounts.oven.value = await findOvenPda({ programAddress });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("donor", accounts.donor),
      getAccountMeta("oven", accounts.oven),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSweetenInstructionDataEncoder().encode(
      args as SweetenInstructionDataArgs,
    ),
    programAddress,
  } as SweetenInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountDonor,
      InstructionAccountInputAddress<TAccountDonor>
    >,
    ResolvedInstructionAccountMeta<
      TAccountOven,
      InstructionAccountInputAddress<TAccountOven>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >);
}

export type SweetenInput<
  TAccountDonor extends InstructionSignerInput = InstructionSignerInput,
  TAccountOven extends InstructionAccountInput = InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput =
    InstructionAccountInput,
> = {
  donor: TAccountDonor;
  oven: TAccountOven;
  systemProgram?: TAccountSystemProgram;
  amount: SweetenInstructionDataArgs["amount"];
};

export function getSweetenInstruction<
  TAccountDonor extends InstructionSignerInput,
  TAccountOven extends InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput,
  TProgramAddress extends Address = typeof LAST_BATCH_PROGRAM_ADDRESS,
>(
  input: SweetenInput<TAccountDonor, TAccountOven, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): SweetenInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountDonor,
    InstructionAccountInputAddress<TAccountDonor>
  >,
  ResolvedInstructionAccountMeta<
    TAccountOven,
    InstructionAccountInputAddress<TAccountOven>
  >,
  ResolvedInstructionAccountMeta<
    TAccountSystemProgram,
    InstructionAccountInputAddress<TAccountSystemProgram>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LAST_BATCH_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    donor: { value: input.donor ?? null, isSigner: true, isWritable: true },
    oven: { value: input.oven ?? null, isSigner: false, isWritable: true },
    systemProgram: {
      value: input.systemProgram ?? null,
      isSigner: false,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("donor", accounts.donor),
      getAccountMeta("oven", accounts.oven),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSweetenInstructionDataEncoder().encode(
      args as SweetenInstructionDataArgs,
    ),
    programAddress,
  } as SweetenInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountDonor,
      InstructionAccountInputAddress<TAccountDonor>
    >,
    ResolvedInstructionAccountMeta<
      TAccountOven,
      InstructionAccountInputAddress<TAccountOven>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >);
}

export type ParsedSweetenInstruction<
  TProgram extends string = typeof LAST_BATCH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    donor: TAccountMetas[0];
    oven: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: SweetenInstructionData;
};

export function parseSweetenInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSweetenInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      donor: getNextAccount(),
      oven: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSweetenInstructionDataDecoder().decode(instruction.data),
  };
}
